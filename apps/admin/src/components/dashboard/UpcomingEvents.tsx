'use client'
import React from 'react'
export function UpcomingEvents() {
  return (
    <div className="bg-white border border-gray-100 rounded-2xl p-6">
      <p className="text-sm font-semibold text-gray-500">UpcomingEvents</p>
      <p className="text-xs text-gray-400 mt-1">Component ready — data loads when API is connected.</p>
    </div>
  )
}
